# Supabase setup for ADS_BOT_BIG

This folder contains local Supabase configuration and seed SQL for the project.

## Usage
- Run Supabase locally if needed.
- Set the `DATABASE_URL` environment variable in Railway or your deployment platform to your Supabase Postgres connection string.
- The application will use PostgreSQL automatically when `DATABASE_URL` is present.
